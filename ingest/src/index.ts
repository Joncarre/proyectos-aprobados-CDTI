// CDTI ingest pipeline (FASE 1).
// Reads the raw JSON files from data/raw, normalises values (amounts, dates,
// booleans, categorical cleanup), computes derived fields and loads everything
// into the DuckDB database. Runs only from the CLI: `npm run ingest`.

console.log('[ingest] Pipeline not implemented yet — coming in FASE 1.');
